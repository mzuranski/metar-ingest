# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=metar_obs
DB_USER=metar_user
DB_PASSWORD=changeme

# Logging Configuration
LOG_DIR=/home/ldm/var/logs/metar-ingest
LOG_LEVEL=INFO

# Daemon Configuration
DAEMON_PID_FILE=/home/ldm/var/metar-ingest.pid
DAEMON_WORKING_DIR=/home/ldm/scripts/obs/metar

# Data Retention (days)
DATA_RETENTION_DAYS=30

# Optional: WIS2 Configuration (future use)
WIS2_ENABLED=False
WIS2_ENDPOINT=http://example.com/wis2

# Optional: Batch processing
BATCH_SIZE=100